<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Number Info Lookup</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #0a0a0a;
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    input, button {
      padding: 10px;
      margin: 10px;
      font-size: 16px;
      border-radius: 8px;
      border: none;
    }
    input { width: 250px; }
    button {
      background-color: #ffcc00;
      cursor: pointer;
    }
    pre {
      background: #1a1a1a;
      padding: 15px;
      border-radius: 10px;
      width: 90%;
      max-width: 500px;
      overflow-x: auto;
    }
  </style>
</head>
<body>
  <h1>🔍 Number Info Lookup</h1>
  <input type="text" id="number" placeholder="Enter mobile number" />
  <button onclick="getDetails()">Get Details</button>

  <pre id="result">Result will appear here...</pre>

  <script>
    async function getDetails() {
      const number = document.getElementById("number").value.trim();
      const resultBox = document.getElementById("result");

      if (!number) {
        resultBox.textContent = "⚠️ Please enter a number.";
        return;
      }

      // Using free proxy to bypass CORS (works on GitHub / Telehost)
      const api = `https://truelookup.hideme.eu.org/?number=${number}&token=fuckyoumodiji`;
      const proxyUrl = `https://api.allorigins.win/get?url=${encodeURIComponent(api)}`;

      resultBox.textContent = "⏳ Fetching details...";

      try {
        const response = await fetch(proxyUrl);
        const data = await response.json();

        if (data && data.contents) {
          resultBox.textContent = data.contents;
        } else {
          resultBox.textContent = "❌ No data found.";
        }
      } catch (error) {
        resultBox.textContent = "🚫 Error fetching details: " + error.message;
      }
    }
  </script>
</body>
</html>

